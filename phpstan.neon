includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    level: 8

    paths:
        - src

    checkMissingIterableValueType: false

    ignoreErrors:
        # Orchestra\Testbench uses mixed $app parameter; not our code.
        - '#Parameter \#1 \$app of method .* expects#'
